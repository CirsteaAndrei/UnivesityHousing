<UserControl x:Class="UniversityHousing.Views.DormitoryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:UniversityHousing.ViewModels"
        xmlns:local="clr-namespace:UniversityHousing.Views"
        mc:Ignorable="d"
        Height="550" Width="800">
    <Grid>
        <ListView ItemsSource="{Binding Dormitories}" SelectedItem="{Binding SelectedDormitory}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Dormitory Number" DisplayMemberBinding="{Binding DormitoryNumber}" Width="120"/>
                    <GridViewColumn Header="Fee Amount" DisplayMemberBinding="{Binding FeeAmount}" Width="120"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom">
            <Button Content="Add" Command="{Binding AddDormitoryCommand}" Width="80" Height="30"/>
            <Button Content="Update" Command="{Binding UpdateDormitoryCommand}" Width="80" Height="30"/>
            <Button Content="Delete" Command="{Binding DeleteDormitoryCommand}" Width="80" Height="30"/>
        </StackPanel>
    </Grid>
</UserControl>
